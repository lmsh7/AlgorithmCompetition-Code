#include <bits/stdc++.h>
using namespace std;

const int N = 2e6 + 10;

string s;

int f[N];
int inv_f[N];


int main() {
	int t;
	cin >> t;
	while(t--) {
		
		cin >> s;
		for(int i = 0; i <= s.length() - 1; ++i) {
			for(int j = 0; j <= s.length() - 1; ++j) {
				for(int q - 1)
			}
		}
	}
	return 0;
}